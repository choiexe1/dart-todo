name: dart_todo
description: A sample command-line todo application.
version: 1.0.0
repository: https://github.com/choiexe1/dart-todo

scripts:
  test: dart test -r github && derry reset
  reset: dart run lib/data_source/json_file_data_source.dart

environment:
  sdk: ^3.7.0

# Add regular dependencies here.
dependencies:
  auto_injector: ^1.0.2
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
